Drop screenshots here with these exact filenames (referenced directly in `../index.html`):

- `images.png`: Images tab
- `video.png`: Video tab
- `audio.png`: Audio tab
- `benchmark.png`: Benchmark tab

Roughly 1280×800 (16:10) works best with the window-chrome frame on the page. Missing files
fall back to a labeled placeholder automatically, so the page never looks broken while you're
still collecting screenshots.
